---
name: timeline
description: >
  Open or rebuild the local timeline web viewer. Regenerates timeline.json from the
  current vault state and serves the viewer over HTTP (required — file:// blocks fetch).
  Triggers on: "timeline", "/timeline", "open timeline", "show timeline".
allowed-tools: Read Write Edit Glob Grep Bash
---

# timeline: Open the Web Viewer

Build the timeline data, compile the viewer if needed, and serve it over HTTP.

---

## Behavior

1. **Check if `timeline/dist/index.html` exists.**
   - If not, run: `cd timeline && npm install && npm run build`
   - Note: `npm run build` also runs `build:data` via the `prebuild` hook, and Vite copies `timeline/data/*` into `dist/data/` automatically.

2. **Rebuild if stale.** If `timeline/dist/index.html` is older than any source file in `timeline/src/` or `scripts/build-timeline-data.ts`, run `npm run build` again. Stale dist bundles are the most common reason the viewer renders blank or misformatted.

3. **Start the preview server** (required — opening `dist/index.html` directly via `file://` blocks the `fetch('./data/timeline.json')` call and the app renders blank).

   **Preferred: `scripts/serve-timeline.sh`** — starts the QMD HTTP server on :3001 alongside Vite dev on :5173 so the viewer's search box works. No-ops the QMD side if QMD isn't installed.
   ```bash
   bash scripts/serve-timeline.sh
   ```

   **Fallback: `npm run preview`** — viewer only, no search. Use for a production-built bundle.
   ```bash
   cd timeline && npm run preview
   ```
   Vite prints a URL like `http://localhost:4173/` (preview) or `http://localhost:5173/` (dev).

4. **Open the browser**:
   ```bash
   open http://localhost:5173/  # if using serve-timeline.sh
   open http://localhost:4173/  # if using npm run preview
   ```

5. **Report** what was built:
   - Number of active days in the timeline
   - Whether a rebuild was needed or skipped (mtime check)
   - Whether QMD search is live (curl :3001/query)
   - The preview URL

## Dev mode

For live reload during development:
```bash
cd timeline && npm run dev
```
Then open the URL Vite prints (usually http://localhost:5173). Search will show "QMD server unavailable" unless you start QMD separately via `qmd mcp --http --daemon --port 3001`.

## Data source

The viewer reads `timeline/data/timeline.json`, generated by `scripts/build-timeline-data.ts`. Every capture skill triggers a rebuild after writing to the day node. `vite build` copies `timeline/data/` → `dist/data/` automatically, so a fresh `npm run build` always ships current data.

## Gotchas

- **Never open `dist/index.html` via `file://`.** Modern browsers block `fetch()` against `file://` origins; the app will render a blank page because `useTimelineData` can't load `timeline.json`. Always serve over HTTP via `serve-timeline.sh`, `npm run preview`, or `npm run dev`.
- **Stale dist.** `npm run build:data` updates `timeline/data/timeline.json` but NOT `dist/data/`. Only `npm run build` (or `vite build`) copies the fresh data into `dist/`. If the viewer shows old data, run a full build.
- **Markdown formatting requires the typography plugin.** `DetailPanel.tsx` uses Tailwind `prose-*` classes which are no-ops unless `@tailwindcss/typography` is listed in `tailwind.config.js` plugins.
- **Search only works via `serve-timeline.sh` or `npm run dev` (not `npm run preview`).** The Vite proxy `/api/qmd → :3001` is only mounted in dev-server mode. Preview mode (production bundle) has no proxy and will silently 404 on search. If you need search with a production bundle, serve both from the same origin or set `QMD_URL` to an absolute URL.
