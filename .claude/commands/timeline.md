---
description: Build and open the local timeline web viewer. Shows your day-by-day activity as a visual timeline.
---

Read the `timeline` skill. Then build the data and open the viewer.

Usage:
- `/timeline` — rebuild data and open in browser
- For dev mode: `cd timeline && npm run dev`
